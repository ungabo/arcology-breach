# Concept Replica Gear Key Set14 - Implementation Plan

Generated: `2026-05-25`

## Scope
Gear Key replica pass B is a focused Unity-only silhouette/proportion correction pass. It is not a broad object family and does not touch main scenes, gameplay scripts, shared status docs, or the main package manifest.

## Current Checkpoint
- Created the Set14 sidecar package by isolating the Set13 package structure under `AssetPacks/BrassworksBreach.ConceptReplicaGearKeySet14/`.
- Updated package identity, builder namespace, marker, and package dependency to Set14 / `CRGK14` / `0.1.58-p014`.
- Patched the Unity builder toward the requested correction: thinner gear bow, 18 smaller irregular teeth, larger open negative space, smaller hub, slimmer spokes, longer thinner shaft, finer collars, compact lower-side bit, darker aged brass material values, no label text in the beauty render, and Set14 render filenames.
- Cleared copied Set13 `.meta` files inside the Set14 package so Unity can mint unique Set14 metadata on import.

## Not Yet Run
Unity generation and render export were not run before shutdown. Existing copied runtime assets are not accepted as final Set14 output until regenerated by the Set14 builder.

## Exact Next Fix
Run the Set14 validation project in Unity batchmode, execute `BrassworksBreach.ConceptReplicaGearKeySet14.Editor.ConceptReplicaGearKeySet14Builder.GenerateAssetsAndRenders`, inspect the three PNG outputs, then tune the gear-bow ring thickness/tooth size again if the silhouette still reads chunky.
