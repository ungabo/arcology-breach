# Preview Scene Notes

Use the package menu commands to generate Level Dressing Set 01 runtime assets and render review contact sheets. The package-local validation project can run the same generator in Unity batchmode without changing the primary game project.
