# Prefab Palette Notes

Import the package into an empty validation project and inspect Runtime/Prefabs. Do not drag these into production scenes until integration creates scene-local AUTH gameplay objects.
