# Weapon Viewmodel Set 03 Preview Notes

Import this package into its `ValidationProject~` or another isolated Unity project, then run the package menu items to generate assets, render previews, and write validation evidence.

No generated prefab contains colliders, gameplay scripts, audio sources, or autonomous runtime behavior.
