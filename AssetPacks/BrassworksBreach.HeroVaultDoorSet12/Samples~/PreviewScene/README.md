# Preview Notes

Import the local package into a scratch Unity project and open the runtime prefabs under `Runtime/Prefabs`. The generated PNG renders under `Documentation/ConceptRenders/V0_1_57_HeroVaultDoorSet12` are reference-only and are not intended for game import.
