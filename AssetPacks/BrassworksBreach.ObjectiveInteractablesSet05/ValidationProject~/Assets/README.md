# Objective Interactables Set 05 Validation Project

This minimal Unity project references the parent sidecar package by local file path so generation, preview rendering, and validation can run without editing the main Brassworks Breach `Packages/manifest.json`.
