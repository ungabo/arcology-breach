# Pipe Tank Gauge Set 10 Preview Notes

Import the package and inspect the prefabs in `Runtime/Prefabs`. The package intentionally contains visual-only meshes and materials with no gameplay components or physics setup.
