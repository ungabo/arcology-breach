# Encounter Enemy Set 02 Preview Notes

Preview renders are generated by the package editor tooling. They are lookdev evidence only and should not be promoted as gameplay scenes.

Each generated prefab includes empty `SOCKET_*` transforms under `future_rig_sockets` for later rig, VFX, muzzle, eye, tank, and weapon hookup.
