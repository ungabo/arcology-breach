# Room Setpiece Kit 04 Validation Project

This project exists only to import the package as a local UPM dependency and run the package-local Unity generator/preview validation. It must not be promoted into the primary game project.
